this is my code review for auth_utils.py

full code: 
from fastapi import Depends, HTTPException
from fastapi.security import OAuth2PasswordBearer
from passlib.context import CryptContext
from jose import jwt, JWTError
from datetime import datetime, timedelta
from dotenv import load_dotenv
import os

# BASE_DIR is the root folder, wherein the .env file is located
BASE_DIR = os.path.dirname(os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__)))))
dotenv_path = os.path.join(BASE_DIR, ".env")

print(f"Looking for .env at: {dotenv_path}")
print(f".env exists: {os.path.exists(dotenv_path)}")

load_dotenv(dotenv_path)

SECRET_KEY = os.getenv("SECRET_KEY")
print("Secret Key loaded")
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 60

oauth2_scheme = OAuth2PasswordBearer(tokenUrl = "/api/login")

pwd_context = CryptContext(schemes = ["bcrypt"], deprecated = "auto")

def hash_password(password: str) -> str:
    return pwd_context.hash(password)

def verify_password(password: str, hashed_password: str) -> bool:
    return pwd_context.verify(password, hashed_password)

def create_access_token(data: dict, expires_delta: int | None = None) -> str:
    to_encode = data.copy()
    expire = datetime.utcnow() + timedelta(minutes = expires_delta or ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, SECRET_KEY, algorithm = ALGORITHM)

def get_current_user(token: str = Depends(oauth2_scheme)) -> int:
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
        user_id: str | None = payload.get("sub")
        if user_id is None:
            raise HTTPException(
                status_code=401,
                detail="Invalid authentication token",
                headers={"WWW-Authenticate": "Bearer"},
            )
        return int(user_id)
    except JWTError:
        raise HTTPException(
            status_code=401,
            detail="Invalid authentication token",
            headers={"WWW-Authenticate": "Bearer"},
        )

explanation with code snippets:

from fastapi import Depends, HTTPException
from fastapi.security import OAuth2PasswordBearer
from passlib.context import CryptContext
from jose import jwt, JWTError
from datetime import datetime, timedelta
from dotenv import load_dotenv
import os

these are my imports 

BASE_DIR = os.path.dirname(os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__)))))
dotenv_path = os.path.join(BASE_DIR, ".env")
print(f"Looking for .env at: {dotenv_path}")
print(f".env exists: {os.path.exists(dotenv_path)}")

load_dotenv(dotenv_path)

this code block locates my backend's .env and loads it using load_dotenv

SECRET_KEY = os.getenv("SECRET_KEY")
print("Secret Key loaded")
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 60

loading the .env is essential since my SECRET_KEY is inside the .env to hide it from the eyes of public. it alsoi uses the algorithm HS256 which is an algorithm that is common in web dev. i also set an expiration for my token for 60 mins as ACCESS_TOKEN_EXPIRE_MINUTES so that in every 60 minutes it will force the user to re-login

oauth2_scheme = OAuth2PasswordBearer(tokenUrl = "/api/login")
pwd_context = CryptContext(schemes = ["bcrypt"], deprecated = "auto")

this code lines are for my oauth2's scheme and its url.
and for my pwd_context using CryptContext with bcrypt for its schemes and depracated set to auto

def hash_password(password: str) -> str:
    return pwd_context.hash(password)

def verify_password(password: str, hashed_password: str) -> bool:
    return pwd_context.verify(password, hashed_password)

def create_access_token(data: dict, expires_delta: int | None = None) -> str:
    to_encode = data.copy()
    expire = datetime.utcnow() + timedelta(minutes = expires_delta or ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, SECRET_KEY, algorithm = ALGORITHM)

created a method called has_password for hashing the string password and returning it as the hashed password using pwd_context.hash.
another method for verifying the password as verify_password it checks whether the password and the hashed password is set to a string type variable, and if it is it returns the password and hashed_password using the pwd_context.verify() method.
another method is created for creating an access token using create_access_token using the parameter data as a dict, expres_delta as int or None and returns it as string.

def get_current_user(token: str = Depends(oauth2_scheme)) -> int:
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
        user_id: str | None = payload.get("sub")
        if user_id is None:
            raise HTTPException(
                status_code=401,
                detail="Invalid authentication token",
                headers={"WWW-Authenticate": "Bearer"},
            )
        return int(user_id)
    except JWTError:
        raise HTTPException(
            status_code=401,
            detail="Invalid authentication token",
            headers={"WWW-Authenticate": "Bearer"},
        )
        
the last method in auth_utils is for getting the current user, and it is named as get_current_user it has a parameter token which is a string and that it depends in oauth2_scheme and it should return an integer. and inside this method it checks for the payload using jwt.decode and this is reliant to the SECRET_KEY (in my .env file), and the algorithms that is reliant to my variable ALGORITHM
it also checks for the integrity and validity of the user_id, if it is valid it returns the user's id, if not it raise an error using HTTPException

Missed Information:

oauth2_scheme = OAuth2PasswordBearer(tokenUrl = "/api/login")
pwd_context = CryptContext(schemes = ["bcrypt"], deprecated = "auto")

OAuth2PasswordBearer doesn't authenticate anything by itself, it only extracts the Authorization: Bearer <token> header. bcrypt automatically salts each password and is resistant to brure-force attacks, which is why FastAPI's standard choice is CryptContext with bcrypt. JWT best practices: "exp" field prevents token reuse after expiration, HS256 is symmetric, so the SECRET_KEY must always stay private.

def get_current_user(token: str = Depends(oauth2_scheme)) -> int:

get_current_user returns an INT because user_id inside JWT is only used as a reference to DB users. JWT shoul never contain sensitive info (like passwords)